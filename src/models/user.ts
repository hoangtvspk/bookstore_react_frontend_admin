export interface LoginForm{
    email:string;
    password:string;
}

export interface UserInfo {
    firstName: string;
    lastName:string;
    email: string;
    phoneNumber: number;
    token: string;
    userRole: string;
}